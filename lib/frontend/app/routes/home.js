import Ember from 'ember';

export default Ember.Route.extend({
  model: function() {
    // var directories = this.store.find('directory', {path: 'home'});
    // console.log('mmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmmm');  // DEBUG
    // console.log(JSON.stringify(directories));  // DEBUG
    // return directories;

    // return this.store.find('directory', {path: '/var'});

    // return [];
  }
});
